Flask==3.0.3
Werkzeug==3.0.2
Jinja2==3.1.4
MarkupSafe==2.1.5
itsdangerous==2.1.2

# Manejo de CORS (por si se usa desde móvil o Google Sites)
flask-cors==4.0.1

# Manejo de variables .env
python-dotenv==1.0.1

# SQLite (ya viene con Python, pero agrego soporte adicional opcional)
pysqlite3-binary==0.5.2

# Para producción (opcional si despliegas en Render / Railway / VPS)
gunicorn==22.0.0

# Para JSON avanzado
simplejson==3.19.2
